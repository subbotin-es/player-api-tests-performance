# Performance thresholds — reference only, not enforced by JMeter natively
# Use these values when evaluating HTML Dashboard results manually

# Response time thresholds (milliseconds)
threshold.p95.smoke=2000
threshold.p95.baseline=1500
threshold.p95.stress=3000

threshold.p99.smoke=5000
threshold.p99.baseline=3000
threshold.p99.stress=8000

# Error rate thresholds (percent)
threshold.error.smoke=0
threshold.error.baseline=1
threshold.error.stress=5

# Throughput floor (requests/second)
threshold.throughput.baseline=5
